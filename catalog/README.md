# catalog

`kpi-registry.yaml` (recipes), `data-dictionary.yaml` (generated at step 7) and `acceptance.yaml` (finance's numbers, written by `/verify`). All three are created by the skills; none is written by hand. Contracts: `docs/contracts/`. Empty until the first dashboard reaches step 3. Filled example: `docs/sample/catalog/`.
