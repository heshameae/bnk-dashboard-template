# GENERATED by /data-dictionary in CI. Do not edit. Contract: docs/contracts/data-dictionary.md
generated_at: 2026-09-06T03:12:00+04:00
source: BI_MODEL
views:
  - name: v_balances_daily
    grain: "one row per account per snapshot_date (business days only)"
    grain_proof: { rows: 48213977, distinct_key: 48213977, as_of: 2026-09-02, from: bi_model/proofs/v_balances_daily.md }
    rls: branch_code
    refresh: "daily after CBS load, ~02:30"
    sources: [CBS_ACCT_BAL_DLY, CBS_ACCT, CBS_PROD, CBS_CUST]
    columns:
      - { name: snapshot_date,    type: DATE,         from: CBS_ACCT_BAL_DLY.BAL_DT,       description: "Business day of the balance" }
      - { name: account_id,       type: VARCHAR2(20), from: CBS_ACCT_BAL_DLY.ACCT_NO,      description: "Account number" }
      - { name: customer_id,      type: VARCHAR2(12), from: CBS_ACCT.CUST_ID,              description: "Primary customer" }
      - { name: branch_code,      type: VARCHAR2(6),  from: CBS_ACCT.BR_CD,                description: "Owning branch (RLS)" }
      - { name: product_family,   type: VARCHAR2(20), from: CBS_PROD.PROD_FMLY,            description: "CASA, TD, LOAN, CARD" }
      - { name: customer_segment, type: VARCHAR2(20), from: CBS_CUST.SEG_CD,               description: "RETAIL, SME, CORPORATE, PRIVATE, UNKNOWN" }
      - { name: account_status,   type: CHAR(1),      from: CBS_ACCT.ACCT_STS,             description: "A active, D dormant, C closed" }
      - { name: balance_amount,   type: "NUMBER(18,2)", from: CBS_ACCT_BAL_DLY.LDGR_BAL_AMT, description: "Ledger balance, AED equivalent" }
      - { name: available_amount, type: "NUMBER(18,2)", from: CBS_ACCT_BAL_DLY.AVL_BAL_AMT,  description: "Available balance, AED equivalent" }
      - { name: currency,         type: CHAR(3),      from: CBS_ACCT_BAL_DLY.CCY_CD,       description: "Account currency" }
    used_by: [kpi.casa_balance, kpi.total_balance, kpi.casa_accounts, kpi.casa_share]
  - name: dim_branch
    grain: "one row per branch"
    grain_proof: { rows: 212, distinct_key: 212, as_of: 2026-09-04, from: bi_model/proofs/dim_branch.md }
    rls: none
    refresh: "daily with CBS_BRNCH"
    sources: [CBS_BRNCH]
    columns:
      - { name: branch_code, type: VARCHAR2(6),  from: CBS_BRNCH.BR_CD }
      - { name: branch_name, type: VARCHAR2(60), from: CBS_BRNCH.BR_NM }
      - { name: region_code, type: VARCHAR2(4),  from: CBS_BRNCH.REGN_CD }
      - { name: region_name, type: VARCHAR2(40), from: CBS_BRNCH.REGN_NM }
    used_by: []
  - name: dim_date
    grain: "one row per calendar day"
    grain_proof: { rows: 4018, distinct_key: 4018, as_of: 2026-09-04, from: bi_model/proofs/dim_date.md }
    rls: none
    refresh: "yearly with CBS_CALENDAR"
    sources: [CBS_CALENDAR]
    columns:
      - { name: date_key, type: DATE, from: CBS_CALENDAR.CAL_DT }
      - { name: is_business_day, type: NUMBER(1), from: CBS_CALENDAR.IS_BUS_DAY }
      - { name: month_start, type: DATE, from: derived }
      - { name: is_last_business_day_of_month, type: NUMBER(1), from: derived }
      - { name: holiday_name, type: VARCHAR2(60), from: CBS_CALENDAR.HOL_NM }
    used_by: []
