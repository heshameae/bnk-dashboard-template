# dashboards

One folder per dashboard, named by its id: `business-context.md` (step 2), `design-export/` (step 8), `spec.yaml` (step 9), `queries.sql` (step 10), `verify.md` (step 11). Filled example: `docs/sample/dashboards/cashboard/`.
