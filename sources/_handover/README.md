# Handovers from the data team

One workbook per handover, as received, never edited: `<YYYY-MM-DD>-<SYSTEM>-schema.xlsx`. `/import-schema` reads the newest and writes the `.yaml` files beside this folder plus `../_import-report.md`. Contract: `docs/contracts/sources.md`.
