# bi_model

Clean tables, one `.sql` per view, written by `/build-model draft`. `proofs/` holds each view's proof query and the data engineer's result. Header format: `docs/CONVENTIONS.md`. Empty until the first dashboard reaches step 3. Filled example: `docs/sample/bi_model/`.
