// Content for the architecture app: the eleven steps, the phases, the clarify lists, the executive narrative.
// Single source of truth for words the app shows. docs/WORKFLOW.md is the short table; this is the long form.
// Owners: you = BI engineer + Claude · de = data engineers · biz = business · auto = CI or the DEs' pipeline.

const PHASES = [
  { id: 'in',      label: 'Tables in',          steps: [1],            say: 'The data team hands us descriptions of their tables. We never touch raw data ourselves.' },
  { id: 'model',   label: 'Model & recipes',    steps: [2, 3, 4, 5, 6, 7], say: 'We turn the ask into clean tables and one recipe per number, prove them, and get the meanings confirmed before anything deploys.' },
  { id: 'design',  label: 'Design & contract',  steps: [8, 9],         say: 'We design the page from the confirmed list of numbers, then write the contract the build reads.' },
  { id: 'ship',    label: 'Build & ship',       steps: [10, 11],       say: 'The app is generated from the contract, checked against finance, and shipped with row-level security proven.' },
];

const STEPS = [
  {
    n: 1, id: 'sources', title: 'Intake sources', owner: 'de', ownerNote: 'the data team owns the numbers; you file them',
    lede: 'The data team hands over a dictionary and a profile of each raw table. We write one file per table and read nothing else about raw data ever again.',
    gets: 'The data team\'s export: table and column names, types, comments, plus a profile (row count, distinct count on the key, date range, last load).',
    does: 'Writes one file per raw table with the grain sentence, the key, the profile numbers and every column. Marks the grain proven only when row count equals distinct key count.',
    hands: 'sources/<TABLE>.yaml, plus a gap report naming every table that is not yet proven.',
    skill: { cmd: '/import-schema', status: 'todo' },
    files: ['docs/sample/sources/CBS_ACCT_BAL_DLY.yaml', 'docs/sample/sources/CBS_ACCT.yaml'],
    rule: 'A table whose grain is not proven cannot be modelled. Naming the gap is the deliverable.',
    trap: 'A column list looks like enough. It cannot tell you whether a row is one account-day or one transaction, and that single fact is the difference between a right balance and one nineteen times too big.',
    clarify: [
      { q: 'Which schemas and tables are in scope for release one?', who: 'data team + you', why: 'Everything else stays out of the repo, so the model has a boundary.' },
      { q: 'Is each profile measured or estimated?', who: 'data team', why: 'Optimizer statistics are approximate. A grain claim needs a counted number.' },
      { q: 'Can they export keys (ALL_CONSTRAINTS, ALL_CONS_COLUMNS)?', who: 'data team', why: 'Without keys, every join is a guess we have to prove by counting.' },
      { q: 'Date column, first and last date, last load time, refresh window?', who: 'data team', why: '"Last business day" and every freshness claim rest on this.' },
      { q: 'Is there a read-only replica account, or a named proof-runner?', who: 'data team + security', why: 'With the account, step 4 disappears. Without it, one person is the bottleneck for every model.' },
      { q: 'Which columns are personal or restricted, and are any masked?', who: 'data team + security', why: 'A masked column in a clean table produces a number nobody can reconcile.' },
      { q: 'Is history kept, or is the table a rolling window?', who: 'data team', why: 'A ninety-day chart on a four-month table works; a three-year one does not.' },
      { q: 'Who re-profiles when their tables change, and how do we hear about it?', who: 'data team', why: 'A silent schema change is the one failure our checks cannot catch in advance.' },
    ],
  },
  {
    n: 2, id: 'context', title: 'Capture the ask', owner: 'you', ownerNote: 'you, straight after the business meeting',
    lede: 'You paste your meeting notes. Claude fills a fixed template and asks at most seven questions, only about real gaps.',
    gets: 'Your notes from the meeting with the business owner. Messy is fine.',
    does: 'Writes the ask under fixed headings: who it is for, the questions it must answer, each number\'s meaning in the business\'s own words with an owner and a comparison, and what to leave out.',
    hands: 'dashboards/<name>/business-context.md, read by step 3 without guessing.',
    skill: { cmd: '/business-context', status: 'todo' },
    files: ['docs/templates/business-context-request.md', 'docs/sample/dashboards/cashboard/business-context.md'],
    rule: 'Every recipe\'s meaning line is copied from this file. Nothing gets invented later that was not written down here.',
    trap: 'Acceptance values do not belong here. Finance supplies them at step 11, against a model that already exists, so the check is independent of the ask.',
    clarify: [
      { q: 'What exactly counts, and what does not?', who: 'business owner', why: 'The one question that matters. It becomes the recipe\'s excludes line and the filters under it.' },
      { q: 'Whose number is it?', who: 'business owner', why: 'A number with no owner cannot be confirmed at step 6 or defended after go-live.' },
      { q: 'Compared to what: yesterday, last month, last year?', who: 'business owner', why: 'A card with no comparison is half a fact. The comparison is part of the recipe, not the page.' },
      { q: 'Which slices do they need: branch, segment, product?', who: 'business owner', why: 'A slice the recipe does not declare cannot be added later without a model change.' },
      { q: 'Currency, rounding, and how foreign-currency accounts are shown?', who: 'business owner + finance', why: 'Rounding decides whether the acceptance check at step 11 passes or fails.' },
      { q: 'As of when: what is the cut-off, and in which timezone?', who: 'business owner + data team', why: '"Today" means a different number at 6am and 6pm.' },
      { q: 'Daily is enough, or do they expect intraday?', who: 'business owner', why: 'Intraday changes the source, the refresh and the cost, and is a different project.' },
      { q: 'Who sees which rows?', who: 'business owner + security', why: 'It sets the restricted column at step 5 and the roles in the policy file.' },
      { q: 'What is out of scope for release one?', who: 'business owner', why: 'Written exclusions are what stop the first dashboard from never shipping.' },
    ],
  },
  {
    n: 3, id: 'draft', title: 'Draft the model', owner: 'you', ownerNote: 'Claude drafts, nothing runs yet',
    lede: 'Claude reads the ask and the source files and drafts the clean tables and recipes this dashboard needs. No database is touched.',
    gets: 'business-context.md, every sources/*.yaml, and whatever clean tables and recipes already exist.',
    does: 'Reuses an existing view or confirmed recipe before writing a new one. Drafts one clean table per subject at one grain, one DRAFT recipe per number, and a proof pack of read-only queries for someone with database access.',
    hands: 'Draft bi_model/*.sql, DRAFT recipes, bi_model/proofs/*.sql, and a policy stub. Goes to a data engineer.',
    skill: { cmd: '/build-model draft', status: 'todo' },
    files: ['docs/sample/bi_model/v_balances_daily.sql', 'docs/sample/bi_model/dim_date.sql', 'docs/sample/catalog/kpi-registry.yaml', 'docs/sample/bi_model/proofs/v_balances_daily.sql'],
    rule: 'A recipe names exactly one clean table and never joins. Joins and renames live in the view, once.',
    trap: 'Designing tables to match the page. That is the extract habit from the old tools. Views are shaped by subject and grain; the design only ever feeds back as a blocked item at step 9.',
    clarify: [
      { q: 'What is the grain sentence of each new view?', who: 'you', why: 'It goes in the header and everything downstream trusts it.' },
      { q: 'Which tables does it join, and is each one one row per key?', who: 'you, from sources/*.yaml', why: 'Joining to anything else multiplies rows and inflates every total.' },
      { q: 'Does a recipe for this number already exist?', who: 'you', why: 'Two recipes for one number is how two dashboards start disagreeing.' },
      { q: 'Is each number a flow, a snapshot, or a ratio?', who: 'you + the owner', why: 'Flows add up over time, snapshots take the last business day, ratios are recomputed. Getting it wrong is the classic wrong number.' },
      { q: 'How does each written exclusion become a filter?', who: 'you', why: 'The words and the SQL must say the same thing, or the review at step 5 cannot pass.' },
      { q: 'Which rows would an inner join drop, and is that acceptable?', who: 'you + the owner', why: 'A dropped segment silently lowers a total. An Unknown member keeps the money visible.' },
      { q: 'Is the calendar in place, with the business-day flag?', who: 'you + data team', why: '"Last business day" is defined in one view and nowhere else.' },
      { q: 'Which day does the proof use, and is it inside the table\'s date range?', who: 'you', why: 'A weekend or a date outside the window returns nothing and proves nothing.' },
    ],
  },
  {
    n: 4, id: 'prove', title: 'Prove the drafts', owner: 'de', ownerNote: 'a data engineer, or a read-only replica account',
    lede: 'A data engineer runs the proof pack on raw data and pastes the numbers back. Nobody outside their team ever touches raw.',
    gets: 'docs/sample/bi_model/proofs/<view>.sql. A few short read-only queries per view.',
    does: 'Runs each proof and records the results next to it: rows against distinct keys, raw row count against view row count, one day\'s raw total against the view\'s total, and five sample rows.',
    hands: 'docs/sample/bi_model/proofs/<view>.md. The numbers stay in the pull request as evidence.',
    skill: { cmd: 'their SQL client, read-only', status: 'bank' },
    files: ['docs/sample/bi_model/proofs/v_balances_daily.md', 'docs/sample/bi_model/proofs/dim_date.md'],
    rule: 'A fact view proves three things: grain, fan-out, conservation. A dimension proves it is one row per key, plus whatever it derives.',
    trap: 'Skipping a check because the view "obviously" works. The three checks are the only reason anyone should believe the grain sentence.',
    clarify: [
      { q: 'Who runs it, and by when?', who: 'data team lead', why: 'Without a named person this step is where every model waits.' },
      { q: 'Which environment: replica or production?', who: 'data team', why: 'A replica keeps a heavy count off the production system.' },
      { q: 'What happens when a check fails?', who: 'you', why: 'Grain fail means the wrong key. Fan-out means a join multiplied. Conservation means a filter or join dropped rows. Each has a different fix.' },
      { q: 'Can this become an automated read-only job?', who: 'data team + security', why: 'With a replica account the whole step disappears and models stop queueing.' },
    ],
  },
  {
    n: 5, id: 'review', title: 'Finalize and review', owner: 'you', ownerNote: 'your judgment, moment one of two',
    lede: 'Claude fixes what the numbers revealed and opens the pull request. You review the way the checklists say. The machine checks form; you check truth.',
    gets: 'The proof results and the drafts.',
    does: 'Corrects keys and joins, stamps each view header with its grain sentence and its restricted column, adds the row-level-security policy, runs the lint, opens the pull request.',
    hands: 'An open pull request: views with headers, DRAFT recipes, the proof evidence, the policy entries.',
    skill: { cmd: '/build-model finalize · /rls · registry-lint', status: 'todo' },
    files: ['.github/PULL_REQUEST_TEMPLATE.md', 'docs/sample/security/rls-policies.yaml', '.github/CODEOWNERS'],
    rule: 'Eight checks per view, three questions per recipe. A view with no policy entry cannot be queried at all.',
    trap: 'Letting the lint stand in for the review. It can tell you a column exists; it cannot tell you the meaning is the business\'s sentence.',
    clarify: [
      { q: 'Which column carries row-level security on each view, or why none?', who: 'you + data owner', why: 'It is the line that makes the view queryable, and security reviews it by code ownership.' },
      { q: 'Which roles see everything, which see only what they are entitled to?', who: 'security + business owner', why: 'It is the policy, and it is a rule, so it lives in git and gets reviewed.' },
      { q: 'Is the meaning still the business\'s own sentence?', who: 'you', why: 'A paraphrase at this point is a different number with the same name.' },
      { q: 'Is the aggregation class right for this grain?', who: 'you', why: 'A snapshot summed across days is the error this whole process exists to prevent.' },
      { q: 'Is every exclusion a decision, not a blank?', who: 'you', why: 'A blank exclusion is an unasked question that surfaces after go-live.' },
      { q: 'Who owns the code review on the security lines?', who: 'security', why: 'Separation of duties: the person modelling the number is not the person approving who sees it.' },
    ],
  },
  {
    n: 6, id: 'confirm', title: 'Confirm meanings', owner: 'biz', ownerNote: 'the business owner reads, you merge',
    lede: 'The business owner reads each draft recipe in plain words and says that is what I meant. Then you merge, and merging is the handover to the data engineers.',
    gets: 'The catalog page rendered from the pull request branch: each recipe as a sentence, its clean table, what it excludes.',
    does: 'Reads and confirms, or corrects. No tool and no SQL; words only.',
    hands: 'Recipes move from DRAFT to CONFIRMED, with who confirmed and when. You merge.',
    skill: { cmd: 'the Catalog page', status: 'todo' },
    files: ['docs/sample/catalog/kpi-registry.yaml'],
    rule: 'A confirmed recipe is the unit of trust in the platform. It is confirmed once, not once per dashboard.',
    trap: 'Merging with a draft recipe still on the page. The confirmation is the whole point of the step.',
    clarify: [
      { q: 'Who confirms each number, by name of role?', who: 'business owner', why: 'One person per number, recorded in the file, so the trail exists a year later.' },
      { q: 'Where do they read it: a preview link or the catalog page?', who: 'you', why: 'Release one can use the pull-request preview; the catalog tab replaces it in phase two.' },
      { q: 'What happens to a correction?', who: 'you', why: 'A corrected meaning goes back to step 3, because the filters change with it.' },
      { q: 'Is a confirmation needed again when a recipe changes?', who: 'business owner', why: 'Yes for meaning or exclusions; no for a performance-only change. Say so once and hold to it.' },
    ],
  },
  {
    n: 7, id: 'deploy', title: 'Deploy and document', owner: 'auto', ownerNote: 'the data engineers\' pipeline; nobody types anything',
    lede: 'Their pipeline reads the merged views from git and creates them in Oracle. Then the dictionary regenerates itself from what is live.',
    gets: 'The merged pull request.',
    does: 'Creates or replaces the views in the reporting schema, then documents them: columns, verified grain, refresh time, restricted column, and which recipes use each view.',
    hands: 'Live clean tables and catalog/data-dictionary.yaml.',
    skill: { cmd: '/data-dictionary (in CI)', status: 'todo' },
    files: ['docs/sample/catalog/data-dictionary.yaml'],
    rule: 'Deployment happens from git only. Nothing in a Claude session ever writes to the database.',
    trap: 'Treating slowness as a modelling problem. Performance lives here: an index, a materialized view, or Oracle keeping the hot tables in memory. Same SQL, same views.',
    clarify: [
      { q: 'Who owns the deploy pipeline, and what triggers it?', who: 'data team', why: 'It is the only path from git to the database, so its trigger is part of the contract.' },
      { q: 'Which schema, and who has select on it?', who: 'data team + security', why: 'The app needs a read-only account on exactly that schema.' },
      { q: 'How do we roll back a bad view?', who: 'data team', why: 'A revert in git plus a redeploy, or their own procedure. Decide before the first incident.' },
      { q: 'Which tables get an index on the restricted column?', who: 'data team', why: 'Every query the platform writes filters on it. It is the cheapest performance win available.' },
      { q: 'Does Oracle also filter underneath, and who decides?', who: 'data team + security', why: 'Belt and braces stack safely, but the decision is theirs and it must be re-applied on materialized views.' },
    ],
  },
  {
    n: 8, id: 'design', title: 'Design the page', owner: 'you', ownerNote: 'you, in the design tool',
    lede: 'Lay the page out with the chart kit. Every number on it is picked from the confirmed list, never invented.',
    gets: 'The catalog: the recipe book shown as a table. Six rows on the first dashboard, sixty by the twentieth.',
    does: 'Composes the page from kit components and tags each chart with the recipe it shows. Layout, not arithmetic.',
    hands: 'The design export. Read once at step 9 and never again.',
    skill: { cmd: 'Open Design + chart kit', status: 'ok' },
    files: ['docs/sample/dashboards/cashboard/design-export/src/Cashboard.tsx'],
    rule: 'A card carrying a real recipe id binds itself at step 9. A card with only a label blocks.',
    trap: 'Inventing a number in the design because it would look good. It becomes a blocked item and a trip back to step 3.',
    clarify: [
      { q: 'Which confirmed numbers go on this page?', who: 'you + business owner', why: 'Picking from the list is what keeps the page and the model in step.' },
      { q: 'Does every card carry a recipe id?', who: 'you', why: 'Without it the binding at step 9 is a guess, and guessing is what blocks.' },
      { q: 'What does it look like on a phone?', who: 'you', why: 'Branch managers open it on a phone. Design for that first.' },
      { q: 'Which slice does each chart use, and is it declared on the recipe?', who: 'you', why: 'An undeclared slice is a model change, not a layout change.' },
      { q: 'Do the labels use the business\'s words?', who: 'you', why: 'A label that disagrees with the recipe\'s meaning restarts the argument the platform exists to end.' },
    ],
  },
  {
    n: 9, id: 'spec', title: 'The contract', owner: 'you', ownerNote: 'your judgment, moment two of two',
    lede: 'Claude binds each chart to one recipe and shows you the table. You confirm it in one pass. This is the only loop in the flow.',
    gets: 'The design export, the recipe book, the dictionary.',
    does: 'Writes one line per chart: this chart shows this recipe, by this slice, with this comparison. A chart whose number is not a recipe yet makes the contract blocked.',
    hands: 'dashboards/<name>/spec.yaml. Ready goes to the build; blocked goes back to step 3 with the missing number named.',
    skill: { cmd: '/spec', status: 'ok' },
    files: ['docs/sample/dashboards/cashboard/spec.yaml'],
    rule: 'The contract holds no formula, no column and no view name. It names recipes.',
    trap: 'Fixing a blocked item by writing arithmetic into the page. That is how the old tools ended up with numbers nobody could trace.',
    clarify: [
      { q: 'Is each binding right, chart by chart?', who: 'you', why: 'One pass over a short table now, instead of a wrong number in production later.' },
      { q: 'Is each blocked item a new number or a design change?', who: 'you + business owner', why: 'The first goes back to step 3; the second goes back to step 8. Different work, different people.' },
      { q: 'Which interactions does the page need?', who: 'you', why: 'A click that filters the page is a line in the contract, not code someone writes.' },
      { q: 'What are the default filters when the page opens?', who: 'business owner', why: 'The default decides the first number every person sees.' },
      { q: 'Can the ready part of the page ship while one card is blocked?', who: 'business owner', why: 'Usually yes, and saying so keeps release one moving.' },
    ],
  },
  {
    n: 10, id: 'build', title: 'Build and preview', owner: 'you', ownerNote: 'you and Claude; the business clicks around',
    lede: 'Backend and frontend are generated from the contract. The business previews a real URL. A change they ask for is an edit to the contract, never to the code.',
    gets: 'spec.yaml. The design export is not read.',
    does: 'Generates the app. For every recipe, one function writes the SQL and adds the user\'s row filter, so no dashboard carries its own arithmetic.',
    hands: 'A running app on a development URL, plus the compiled SQL saved for review.',
    skill: { cmd: '/build', status: 'todo' },
    files: ['docs/sample/dashboards/cashboard/queries.sql', 'docs/sample/security/test-users.yaml'],
    rule: 'One function adds the row filter, and it fails closed: no entitlements means no rows, never all rows.',
    trap: 'Hand-editing a generated page. The next build overwrites it, and the contract stops describing what is live.',
    clarify: [
      { q: 'Which test users, with which entitlements?', who: 'you + security', why: 'Two entitled users, one bank-wide, one with nothing. That set proves the filter works.' },
      { q: 'Who may open the development URL?', who: 'security', why: 'It shows real numbers, so it needs the same login as production.' },
      { q: 'How is a draft number shown in the preview?', who: 'you', why: 'A visible badge stops an unconfirmed number from being quoted in a meeting.' },
      { q: 'Where does preview feedback go?', who: 'you', why: 'Into the contract, so the next build carries it and nothing is lost in chat.' },
      { q: 'Is the compiled SQL reviewed before go-live?', who: 'you + data team', why: 'It is the exact text Oracle will run, and reading it once catches a surprising amount.' },
    ],
  },
  {
    n: 11, id: 'verify', title: 'Verify and ship', owner: 'you', ownerNote: 'you; finance supplies the numbers',
    lede: 'Every number is checked against the values finance would bet money on. The row-security test runs. Any miss blocks the release and names the step to go back to.',
    gets: 'The running app, finance\'s values with their dates and source, and the test users.',
    does: 'Compiles each number for the given date and compares. Checks that two entitled users see different totals, that bank-wide equals the unfiltered total, and that an unentitled user sees nothing.',
    hands: 'dashboards/<name>/verify.md, and a live dashboard when it is green.',
    skill: { cmd: '/verify (in CI)', status: 'todo' },
    files: ['docs/sample/dashboards/cashboard/verify.md', 'docs/sample/catalog/acceptance.yaml'],
    rule: 'Finance\'s number is written beside the recipe in catalog/acceptance.yaml, never inside it, so every future build re-checks it automatically.',
    trap: 'Accepting close enough. The tolerance belongs to the number\'s format and is decided once, not argued per release.',
    clarify: [
      { q: 'Which value, as of which date, from which report?', who: 'finance', why: 'A number without a date and a source cannot settle a disagreement.' },
      { q: 'What tolerance does each format allow?', who: 'finance + you', why: 'Rounding to millions and an exact count are different tests.' },
      { q: 'Who approves go-live?', who: 'business owner + security', why: 'One name, recorded, for a dashboard that will be quoted in board packs.' },
      { q: 'Who owns the number after go-live, and who is called when it looks wrong?', who: 'business owner + you', why: 'The trail is the recipe, the proof and this file, so the answer is a five-minute read, not a week.' },
      { q: 'What is monitored: freshness, query log, failed logins?', who: 'data team + security', why: 'A stale dashboard is worse than a missing one, because people trust it.' },
    ],
  },
];

const EXEC = {
  headline: 'One place where every number has an owner, a definition and a proof.',
  problem: [
    'The old tools are being retired, and with them the habit of each report defining its own numbers.',
    'Two dashboards could show a different total for the same day, and settling it meant a week of tracing spreadsheets.',
    'Access was per report, so who could see which branch was a matter of memory.',
  ],
  answer: [
    ['Clean tables', 'One saved query per subject in the database, with the grain written down and proven by counting.'],
    ['Recipes', 'One definition per number, in the business\'s own words, confirmed by the number\'s owner before anything is built.'],
    ['One query writer', 'Every dashboard asks the same component for its numbers, and that component adds each user\'s row filter. No dashboard carries its own arithmetic.'],
    ['One portal', 'Dashboards, a catalog of every number and where it comes from, self-service exploration, and an admin screen for access.'],
  ],
  changes: [
    ['A number means one thing', 'Confirmed once by its owner, then reused by every dashboard.'],
    ['A dashboard is days, not months', 'Once the numbers exist, the next dashboard is layout plus a contract.'],
    ['Access is granted, not rebuilt', 'One table of entitlements, edited in the portal with two-person approval and an audit trail.'],
    ['Disagreements end in minutes', 'Recipe, proof and verification sit next to each other in version control.'],
  ],
  controls: [
    'Every database access from the tooling is read-only. Views reach the database only through the data team\'s pipeline, from reviewed and merged code.',
    'Every number carries a proof of its grain and a value from finance it is checked against on every build.',
    'Row-level security is one reviewed function, with rules in version control, values in an audited table, and a two-user test that blocks release.',
    'Separation of duties by code ownership: the person defining a number is not the person approving who may see it.',
  ],
  needs: [
    ['Data team', 'A dictionary and profile per table, a keys export, and a read-only replica account. The last one removes the biggest queue in the process.'],
    ['Security', 'The role mapping, the approvers for access grants, and a decision on filtering inside the database.'],
    ['Finance', 'Five numbers with dates and a source. They become the automatic check on every future release.'],
    ['Business owners', 'Thirty minutes each to confirm what their numbers mean.'],
  ],
  phases: [
    ['Phase one · the factory', 'The eleven steps working end to end for the first dashboard, with row-level security and the admin screen.'],
    ['Phase two · the catalog', 'Every number and clean table visible, with lineage from raw table to dashboard. Owners confirm meanings there.'],
    ['Phase three · exploration', 'Analysts build their own views from governed numbers, through the same query writer and the same security.'],
  ],
};

module.exports = { PHASES, STEPS, EXEC };
