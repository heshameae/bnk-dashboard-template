# security

`rls-policies.yaml` (roles and per-view policies, written by `/rls`), `test-users.yaml`, `entitlements.sql`. Contract: `docs/contracts/rls-policies.md`; the model: `docs/rls.md`. Empty until the first view is drafted. Filled example: `docs/sample/security/`.
